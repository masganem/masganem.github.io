import { css } from "@emotion/css";
import { fonts } from "../../styling/fonts";

export const baseText = css`
  font-family: ${fonts.family.primary}, 'monospace';
  font-size: medium;
  font-weight: ${fonts.weight.medium};
`;